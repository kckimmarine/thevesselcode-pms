# Local-only audit uploads (PDF, scans, contracts) — never commit
06-records-evidence/incoming/**
!06-records-evidence/incoming/.gitkeep
!06-records-evidence/incoming/INDEX.md

# Drafts with personal or commercial secrets
**/*.draft.*
**/*-CONFIDENTIAL.*

# OS / editor
.DS_Store
Thumbs.db
