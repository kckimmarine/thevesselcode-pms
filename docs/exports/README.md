# Exported context bundles

These files are generated for Gemini / review handoff. Regenerate with:

```bash
node scripts/export-context-for-gemini.mjs
```

Or the frontend-only bundle:

```bash
node scripts/export-tvc-core-frontend.mjs
```
