# Dependencies
node_modules/

# Logs
*.log
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db

# Temp
*.tmp
gitstatus.tmp

# Electron / Pilot license secrets & build output
electron/keys/private.pem
dist/
build/licenses/
build/electron-builder/
*.exe
!downloads/TVC-PMS-VESSEL_*-Setup.exe
*.blockmap
*.nsis.7z

# Local Admin release archives (Setup / App Update ZIP); keep folder via .gitkeep
admin/releases/**
!admin/releases/.gitkeep

# Deploy secrets (Supabase / Vercel tokens — never commit)
deploy/.env.deploy.local
.env
deploy/.env.local

# Playwright HTML/trace dumps (keep e2e/artifacts screenshots + TEST_REPORT.md)
/test-results/
/playwright-report/
/blob-report/
