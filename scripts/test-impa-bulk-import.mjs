/**
 * Smoke test: IMPA bulk import (CSV chunk loader)
 * Run: node scripts/test-impa-bulk-import.mjs
 *
 * Bulk import UI was removed from the PMS shell when STORE moved to toolkit.html.
 */
console.log('SKIP: IMPA bulk import UI is not part of TVC-PMS (catalog is public at /toolkit).');
process.exit(0);
